<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_users_form_webhooks" model="ir.ui.view">
        <field name="name">res.users.form.webhooks</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='access_rights']" position="inside">
                <group string="Webhook Scope" groups="base.group_system">
                    <field name="webhook_allowed_partner_ids" widget="many2many_tags" options="{'no_create': True}" placeholder="No partner-scoped webhook access"/>
                </group>
            </xpath>
        </field>
    </record>

</odoo>